pushd oss
docker compose --profile puppet up -d
popd
#sleep 30
#docker exec oss-puppet-1 puppetserver ca generate --certname puppetboard

# После запуска стека puppet выполните следующую команду для генерации дополнительного сертификата.
# Он будет помещен в том puppetserver-ssl или любой другой том, который вы могли смонтировать для /etc/puppetlabs/puppet/ssl.
#
# docker exec oss-puppet-1 puppetserver ca generate --certname puppetboard
#